// Sum of N numbers by using recursion (Functional way)
// Striver A2Z DSA Course

#include <iostream>
using namespace std;
int sum (int n)
{
    if (n == 0 )
    return 0 ;
    else 
    return n+sum(n-1);
}
int main()
{
    int n =3 ;
    std::cout << sum(n) << std::endl;
    
}
